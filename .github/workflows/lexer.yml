name: Build standalone lexer

on:
  push:

jobs:
  build:

    runs-on: ubuntu-20.04

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: |
        cd 1-lexer
        make
	make test
