int ch(int a, int b, int c, int d, int e, int f, int g, int h) {

int x; x=3;
int y; y=40;
int z; z=4*x;

a = c*c*(3-4*d) - b + a + d + e*f + g*h - (100*h);
f = a-a+4*3*501*7 - 43000;
g = f + 1 * y * z;

return a + b + c - f + g - 2*z*y;

}

int printf();

int main() {
        int a; a=3;
        printf("result should be %d, is %d\n", 27679738, ch(1,2,532,-23,40,0,2,-8086));
	return 0;
}
